package pl.sda.java.adv.school.util;

import pl.sda.java.adv.school.model.Grade;

import java.util.Collection;
import java.util.Optional;

public class GradeUtils {

    public static Optional<Double> gradesAverage(Collection<Grade> grades, String studentId) {
//        grades.stream().filter(grade -> grade.getStudentId().equals(studentId))
//                .map(Grade::getGradeWeight,Grade::getValue)
        return Optional.empty();
    }
}
